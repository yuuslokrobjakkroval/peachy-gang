import { UseFormRender, GiveawayScheduleFeature } from "@/utils/types";

// @ts-ignore
export const useGiveawaysFeature: UseFormRender<GiveawayScheduleFeature> = (
  data: any,
  onSubmit
) => {
  return <div>Hello</div>;
};
