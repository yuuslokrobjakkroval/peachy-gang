import { UseFormRender, AutoResponseFeature } from "@/utils/types";

// @ts-ignore
export const useAutoResponseFeature: UseFormRender<AutoResponseFeature> = (
  data: any,
  onSubmit
) => {
  return <div>Hello</div>;
};
